## 1. Global Components

- [x] 1.1 Create `src/components/ScrollToTop.tsx` with location listener.
- [x] 1.2 Create `src/components/Navbar.tsx` with responsive burger menu.

## 2. Layout Integration

- [x] 2.1 Update `src/App.tsx` to include `ScrollToTop` and `Navbar`.
- [x] 2.2 Adjust global styles or layout spacing for the fixed/sticky Navbar.

## 3. Navigation Refinement

- [x] 3.1 Update `ListaPage.tsx` or `Header.tsx` to ensure visual consistency with the new Navbar.

## 4. Verification

- [x] 4.1 Verify that navigating between pages scrolls the window to the top.
- [x] 4.2 Verify the burger menu appears on mobile views and toggles correctly.
- [x] 4.3 Confirm that all links in the Navbar work as expected.
